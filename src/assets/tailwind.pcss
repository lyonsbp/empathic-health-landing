@tailwind base;
@tailwind components;
@tailwind utilities;

button {
}

button.primary {
  @apply text-white py-1 px-8 rounded-full;
  font-family: "Montserrat", sans-serif;
  background-color: #00c1cd;
}

.email-input::placeholder {
  @apply text-gray-900;
  font-style: italic;
}
